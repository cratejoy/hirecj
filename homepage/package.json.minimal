{
  "scripts": {
    "dev": "PORT=3000 NODE_ENV=development tsx server/index.ts",
    "build": "vite build",
    "start": "NODE_ENV=production tsx server/index.ts",
    "test": "vitest",
    "lint": "eslint . --ext .ts,.tsx",
    "check": "tsc",
    
    "// Monorepo integration": "These defer to root commands",
    "run:dev": "cd .. && make dev-homepage",
    "run:test": "cd .. && make test-homepage",
    "run:build": "cd .. && make build-homepage"
  }
}