# ================================================
# HOMEPAGE SERVICE MINIMAL CONFIG
# ================================================
# Vite requires a .env file in the project root
# This file just maps from shared config to Vite's expected format

# Map from shared config (these will be populated from root .env)
VITE_API_BASE_URL=${AGENTS_SERVICE_URL}
VITE_WS_BASE_URL=ws://localhost:8000
VITE_AUTH_URL=${AUTH_SERVICE_URL}

# Override for production auth URL (you had this set)
VITE_AUTH_URL=https://amir-auth.hirecj.ai