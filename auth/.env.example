# ================================================
# AUTH SERVICE CONFIGURATION
# ================================================
# Most configuration comes from root .env
# Only service-specific overrides go here

# Server Configuration (if different from shared)
# APP_HOST=0.0.0.0
# APP_PORT=8103

# OAuth Redirect Configuration
# These can be overridden per environment
OAUTH_REDIRECT_BASE_URL=http://localhost:8103
FRONTEND_URL=http://localhost:3456
FRONTEND_SUCCESS_PATH=/auth/success
FRONTEND_ERROR_PATH=/auth/error

# ================================================
# SECRETS GO IN .env.secrets
# ================================================
# Copy .env.secrets.example to .env.secrets and add:
# - JWT_SECRET
# - ENCRYPTION_KEY
# - OAuth client IDs and secrets